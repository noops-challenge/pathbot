# Node.JS pathbot solver

You simply need Node.JS installed, then run in the current directory:
```bash
node index.js
```

This bot follows a simple advice when trying to escape mazes : Always follow the right wall
